// Immrdistely Invoked Function Expression (IIFE)

(function chai(){
    //named IIFE
    console.log(`DB Connected`);
})();

    // arrow function IIFE
((name)=>{
    console.log(`DB connected two ${name}`);
} ) ('anshit')

// iife done